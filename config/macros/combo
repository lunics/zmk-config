#define COMBO(NAME, TIMEOUT, POSITION, BINDINGS) \
NAME {\
  layers        = <la_DEF>    ;\
  timeout-ms    = <TIMEOUT>   ;\
  slow-release                ;\
  key-positions = <POSITION>  ;\
  bindings      = BINDINGS    ;\
};

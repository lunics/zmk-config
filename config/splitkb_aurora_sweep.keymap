#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>
#include <dt-bindings/zmk/bt.h>
#include "defines/main.h"
#include "behavior_skq.h"
#include "layers/*"

/ {
  behaviors {
    TEST
  };

  keymap {
    compatible = "zmk,keymap";

    default {
      bindings = <
        LAYER_DEFAULT
    >;};
    numbers {
      bindings = <
        LAYER_NUMBERS
    >;};
    symbols {
      bindings = <
        LAYER_SYMBOLS
    >;};
  };
};

zmk-viewer
  generate -f ~/zmk-config/config/file.keymap cradio
    <empty>           output an image for each layer
    --unified         output one image with all layers stacked
    --single          output one image with all layers
